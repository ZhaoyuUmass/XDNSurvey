extends layout

block content
  h1= title
  div(class="list-group")
    a(class="list-group-item list-group-item-primary") Surveys
    each val,index in survey
      a(href='/survey/'+index class="list-group-item")= 'Survey '+index
